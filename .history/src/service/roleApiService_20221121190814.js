import db from "../models";
const createNewRoles = async (rolse) => {
    try {
    } catch (error) {
        console.log(error);
        return {
            EM: "user not exist",
            EC: 2,
            DT: data,
        };
    }
};

module.exports = { createNewRoles };
