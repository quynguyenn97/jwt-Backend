import db from "../models";
const createNewGroups = async () => {
    try {
    } catch (error) {
        console.log(erro);
        return {
            EM: "user not exist",
            EC: 2,
            DT: data,
        };
    }
};

module.exports = { createNewGroups };
