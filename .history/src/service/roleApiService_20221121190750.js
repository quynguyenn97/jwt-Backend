import db from "../models";
const createNewGroups = async (rolse) => {
    try {
    } catch (error) {
        console.log(error);
        return {
            EM: "user not exist",
            EC: 2,
            DT: data,
        };
    }
};

module.exports = { createNewGroups };
